<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".FakeLogContentProvider"
            android:authorities="${applicationId}.fake"
            android:exported="true">
            <meta-data
                android:name="com.zuhlke.logging.LOGS_PROVIDER"
                android:value="${applicationId}.fake" />
        </provider>
    </application>
</manifest>