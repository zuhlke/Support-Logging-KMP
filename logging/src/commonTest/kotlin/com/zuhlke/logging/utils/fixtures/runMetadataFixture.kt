package com.zuhlke.logging.utils.fixtures

import com.zuhlke.logging.data.RunMetadata

internal val runMetadataFixture = RunMetadata(
    appVersion = "1.0.0",
    osVersion = "15.0",
    device = "Pixel 5"
)